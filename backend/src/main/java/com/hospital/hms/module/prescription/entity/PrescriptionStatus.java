package com.hospital.hms.module.prescription.entity;

/** Mirrors ENUM('Active','Completed','Cancelled') - all legal Java identifiers. */
public enum PrescriptionStatus {
    Active,
    Completed,
    Cancelled
}
