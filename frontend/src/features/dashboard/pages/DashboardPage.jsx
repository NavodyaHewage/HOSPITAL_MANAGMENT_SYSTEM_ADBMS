export default function DashboardPage() {
  return (
    <section>
      <h2>Dashboard</h2>
      {/* KPI cards fed by /reports: today's appointments, outstanding bills,
          expiring batches, pending lab work. */}
    </section>
  );
}
